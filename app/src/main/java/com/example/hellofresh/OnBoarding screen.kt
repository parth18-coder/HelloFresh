package com.example.hellofresh

/*
First create the Array list for the items.
Then add the dependency for the horizontal pager.
Then create the composable for the onBoarding pager. and we have to add one more annotation called @ExperimentalPagerApi
We also have to create the remember page state composable

Now we work on the indicators
 */